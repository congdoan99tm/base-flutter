/// Abstract repository contract for the domain layer.
/// The implementation lives in `lib/data/repositories/`.
///
/// Example:
/// ```dart
/// abstract class SampleRepository {
///   Future<Either<Failure, SampleEntity>> getSample(int id);
/// }
/// ```

// TODO: Define your repository interfaces here.
